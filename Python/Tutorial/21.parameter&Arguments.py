"""
Parameters vs Arguments:

Parameters: These are variables that are defined in the function definition. They act as placeholders for the values you will pass when calling the function.
Example:
def greet(name):  # 'name' is a parameter
    print(f"Hello, {name}!")

    
Arguments: These are the actual values that you pass to the function when calling it. The arguments are assigned to the corresponding parameters.
Example:
greet("Alice")  # 'Alice' is an argument

NOTE: in any function the num of parameter should be equals to number of arguments otherwise it will give error.

"""
