"""
Set Methods:

Method	            Description	                                    Example
add(x)	            Adds item x to the set	                        s.add(5)
remove(x)	        Removes x from set; raises error if missing	    s.remove(2)
discard(x)	        Removes x from set, if present	                s.discard(2)
clear()             clear the entire set at once                    s.clear()
copy()              returns a copy of the specified set.            s.copy()
pop()               removes a random item from the set.             s.pop()

#Optional
union(set)	        Returns union of sets	                        s.union({1, 3})
intersection(set)	Returns intersection of sets	                s.intersection({1, 2})
difference(set)	    Returns difference of sets	                    s.difference({1})

"""

# NOTE: it's not optimum there can be more methods.